{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "npm install --legacy-peer-deps --include=dev && npx expo export --platform web --output-dir dist"
  },
  "deploy": {
    "startCommand": "npx tsx scripts/serve-production.ts",
    "healthcheckPath": "/api/system-status",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
